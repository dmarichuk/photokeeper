from albums.models import Album, Comment
from actstream.models import Action
from users.models import User, Follow

user = User.objects.get(username='test-user')
test_user = User.objects.create(username='test-user1', email='test-user1@g.ty')
Album.objects.create(title='test-title', description='test-description', creator=user)
Comment.objects.create(text='nice pic', creator=user)
Follow.objects.create(user=user, follower=test_user)
actions = Action.objects.all()

for action in actions:
    print(f'{action.timestamp} {action.actor} {action.verb} {action.action_object} {action.target}')

